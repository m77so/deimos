import * as React from 'react'
import { RouteState } from '../modules/route'
import { ActionDispatcher } from '../containers/route'

interface Props {
  value: RouteState
  actions: ActionDispatcher
}

export class Route extends React.Component<Props, {}> {
  render() {
    return (
      
    )
  }
}